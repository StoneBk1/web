<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comment</title>
</head>
<body>
    <h1> Leave a comment</h1>
    <% for (let comment of comments) { %>
        <ul>
            <li><%= comment.comment %> - <b><%= comment.username %> <button><a href="/comments/<%= comment.id %>">Get More Detail</a></button>
            </b></li>
        </ul>
    <% } %>

    <button><a href="/new">Make a new Comment</a></button>
    
</body>
</html>